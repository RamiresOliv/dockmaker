/*
This adds favicon!!!!!!!!!!!!!!!!
*/

// This loads very fast the ico. But can cause issues too. If you think this is bad,
// idk what you should do. (just trust)
const base_favicon_link = document.getElementById("base_favicon_link");
base_favicon_link.href = fixPath("favicon.ico");
